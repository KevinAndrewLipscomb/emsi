create user if not exists 'ki-pacrat-d'@'localhost' identified by '~ki-pacrat-d-password~';
grant  select
 , insert
 , update
 , delete
 , create
 , create routine
 , alter routine
  on emsidb_d.*
  to 'ki-pacrat-d'@'localhost'
;
--
-- Must grant PROCESS priv to primary user to include SHOW ENGINE INNODB
-- STATUS output in MySQL-related exception reports.
--
;
grant process
  on *.*
  to 'ki-pacrat-d'@'localhost'
;
create user if not exists 'ki-pacrath-d'@'localhost' identified by '~ki-pacrath-d-password~';
grant  select
  on homedb_d.*
  to 'ki-pacrath-d'@'localhost'